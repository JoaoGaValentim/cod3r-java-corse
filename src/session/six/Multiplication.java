package session.six;

public class Multiplication implements Calcule {
    public double calculate(double firstValue, double lastValue, Operations operation) {
        return firstValue * lastValue;
    }
}
